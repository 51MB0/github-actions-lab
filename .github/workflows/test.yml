name: Mon premier workflow
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Récupérer le code
        uses: actions/setup-node@v4

      - name: Vérifier le fichier
        run: |
            echo "Github Actions est génial !"
            ls -la
            cat index.html